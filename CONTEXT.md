# OpsFlow

OpsFlow 是面向内网环境的运维集成平台，用于把资产台账、网络探测、链路测速、宽带合同、拓扑、诊断、监控透视和审计放在同一个工作台中。

## Language

**资产**:
被纳入运维管理范围的 IT 设备或网络节点，通常包含名称、IP、类型、位置、负责人、用途和连接凭据状态。
_Avoid_: 设备台账项, 主机记录

**连接凭据**:
用于登录资产的账号、密码、enable 密码或 SSH 私钥等敏感访问材料。
_Avoid_: 密码信息, 登录信息

**在线终端**:
从平台直接连接某个资产的交互式远程会话。
_Avoid_: Web 终端, 远程控制页

**扫描任务**:
对一个目标地址、地址段或端口范围发起的 Nmap 探测请求，用于发现存活主机、开放端口和服务信息。扫描任务必须归属于一个组织，由该组织的探针在本地执行；管理员创建时必须显式指定目标组织，普通用户的任务归属其所在组织。
_Avoid_: Nmap 任务, 探测任务

**扫描目标**:
扫描任务要覆盖的 IP、网段或主机集合。
_Avoid_: 目标, 扫描范围

**性能测试任务**:
对指定 Iperf3 服务端发起的网络吞吐、抖动、丢包或重传测试。性能测试任务必须归属于一个组织，由该组织的探针在本地执行；管理员创建时必须显式指定目标组织，普通用户的任务归属其所在组织。
_Avoid_: Iperf 任务, 测速任务

**宽带合同**:
一条运营商链路的合同和费用记录，包含带宽、位置、续费周期、费用、起止日期和提醒策略。
_Avoid_: 宽带台账, 线路合同

**续费提醒**:
宽带合同到期前按配置的提前天数触发的提醒。
_Avoid_: 到期提醒, 缴费提醒

**拓扑节点**:
网络拓扑图中的一个设备、主机或逻辑节点，可以来自自动发现，也可以由用户手动维护。
_Avoid_: 节点, 图节点

**拓扑连线**:
拓扑节点之间的连接关系，表示链路类型、接口、带宽和来源。
_Avoid_: 边, 链路

**拓扑发现任务**:
面向指定子网发起的发现流程，用于生成或补充拓扑节点与拓扑连线。
_Avoid_: 拓扑扫描, 自动发现

**网络诊断**:
对网络连通性、路径、DNS、端口或链路质量进行即时排障的工具集合。
_Avoid_: 排障工具, 诊断工具

**Zabbix 监控透视**:
平台对内网 Zabbix 数据的统一查看入口，包括大盘、主机和告警事件。
_Avoid_: Zabbix 集成, 监控大盘

**探针**:
部署在某个场地内网中的轻量执行代理，代表该场地的组织在本地执行网络探测和测速任务，并将结果回传至中心服务器。探针不是独立的 OpsFlow 实例，而是中心服务器的远程延伸，仅负责 Nmap 扫描和 Iperf3 测速。
_Avoid_: Agent, 代理客户端, 远程节点

**用户**:
可以登录 OpsFlow 的人员账号，具有管理员、场地管理员或普通用户角色，隶属于一个组织（管理员除外）。
_Avoid_: 账号, 操作员

**组织**:
一个分公司或场地的逻辑边界，用于隔离资产、宽带合同、扫描任务等业务数据。每个组织可以配置独立的探针、钉钉通知和 Zabbix 集成。
_Avoid_: 租户, 场地, 分公司

**审计日志**:
记录用户登录、登出、创建、更新、删除等操作的追踪记录。
_Avoid_: 操作日志, 日志记录

**巡检方案**:
一组可定时执行的检查定义，用于说明要检查哪些对象、执行哪些检查以及异常汇总如何通知。
_Avoid_: 自动化巡检, 巡检配置

**巡检报告**:
一次巡检执行后的结果汇总，包含检查状态、异常摘要和通知状态。
_Avoid_: 巡检结果, 检查报告

**配置备份任务**:
面向一组资产定时拉取设备配置的备份定义。
_Avoid_: 备份计划, 拉配置任务

**配置快照**:
某次配置备份产生的一份设备配置内容记录，可与上一份快照进行差异对比。
_Avoid_: 配置版本, 备份文件

**批量命令任务**:
对一组资产执行同一组网络设备命令并汇总结果的操作记录。
_Avoid_: 批量执行, 命令脚本

**命令结果**:
批量命令任务中单个资产的执行输出、状态和错误信息。
_Avoid_: 执行结果, 回显

**IPAM 子网**:
纳入 IP 地址管理的网络地址段，包含用途、位置、网关、VLAN 和 DHCP 适配策略。
_Avoid_: 子网登记, 地址段

**IP 地址记录**:
IPAM 子网中的单个地址占用记录，可能来自手动维护、扫描发现或 DHCP 环境中的自动发现。
_Avoid_: IP 记录, 地址记录

**运维数据大屏**:
OpsFlow 的全局概览入口，用于聚合资产、扫描、宽带、巡检和其他模块的关键状态。布局固定，指标由后端预聚合。
_Avoid_: 首页, 数据看板

**仪表盘面板**:
运维数据大屏中一个可配置的可视化单元，绑定一个数据源（Zabbix 监控项、iperf3 测速结果、扫描统计等）和一种图表类型（折线、数值、表格等）。面板位置和大小可拖拽调整，查询参数可编辑，按组织隔离存储。不同组织的 Zabbix 主机/接口不同，用户通过面板编辑器显式选择数据源，不在组织表上绑死。
_Avoid_: 小部件, 组件, 图表卡

**面板数据源**:
仪表盘面板查询的数据来源，分三类：Zabbix 监控项（按 host_id + item_key 拉历史值）、iperf3 最近测速（按 org_id 拉最近一条 IperfTask）、扫描统计（按 org_id 拉最近一条 ScanTask）。面板配置存数据库，Zabbix 凭据按组织隔离。
_Avoid_: 数据源配置, 查询定义
